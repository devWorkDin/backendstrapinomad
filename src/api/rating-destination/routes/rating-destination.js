'use strict';

/**
 * rating-destination router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::rating-destination.rating-destination');
