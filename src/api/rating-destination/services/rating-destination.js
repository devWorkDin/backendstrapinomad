'use strict';

/**
 * rating-destination service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::rating-destination.rating-destination');
